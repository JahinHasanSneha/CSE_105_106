#include <bits/stdc++.h>
using namespace std;
int main()
{
    int sum = 0;
    int count = 30;
    while (count <= 30)
    {
        sum = sum + count;
        count++;
        cout << "Sum" << sum << endl;
    }
}